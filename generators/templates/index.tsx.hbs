import * as S from './style';
import { slide } from 'animations';

type Props = {
  title?: string;
};

const {{pascalCase name}} = ({ title = '{{pascalCase name}}' }: Props) => (
  <S.Container variants={slide.down}>
    <S.Title>{title}</S.Title>
  </S.Container>
);

export default {{pascalCase name}};
